return {
  "folke/zen-mode.nvim",
  opts = {
        window = {
    backdrop = 1.95, -- shade the backdrop of the Zen window. Set to 1 to keep the same as Normal

        },
			plugins = {
				gitsigns = true,
				tmux = true,
			},
		},
    -- your configuration comes here
    -- or leave it empty to use the default settings
    -- refer to the configuration section below
}
